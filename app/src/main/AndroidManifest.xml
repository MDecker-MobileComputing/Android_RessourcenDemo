<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RessourcenDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity android:name=".activites.SprachanpassungsActivity" />

        <activity android:name=".activites.ShapeAnzeigeActivity" />

        <activity android:name=".activites.TweenAnimationsActivity" />

        <activity android:name=".activites.FrameAnimationsActivity" />

        <activity android:name=".activites.ArrayRessourcenActivity" />

        <activity android:name=".activites.ZweiQualifiziererActivity" />

    </application>

</manifest>